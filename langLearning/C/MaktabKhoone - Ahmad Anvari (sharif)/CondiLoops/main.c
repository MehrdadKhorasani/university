#include <stdio.h>
#include <stdlib.h>

int main()
{
  /*
  //// IF statement
  // operators in conditions expressions: (== , != , > , < , >= , <=)
  //1.
  int age;
  puts("Please enter your age:1");
  scanf("%d", &age);
  if (age > 18) {
    printf("%d", age);
  }
  //2.
  char c = getchar();
  if (c == 'E') // ATTENTION: Use Single Quote
  {
    puts("Equal");
  }
  puts("end");
  */

  return 0;
}
